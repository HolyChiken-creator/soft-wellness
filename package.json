{
  "name": "soft-wellness",
  "version": "1.0.0",
  "private": true,
  "description": "Local-first nutrition diary PWA deployed to Cloudflare Workers with Wrangler",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
